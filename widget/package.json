{
  "name": "sykkelveier-widget",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "//": "FIXME: Add build script with minification, and maybe even install with --production first?",
    "build": "bun build index.tsx --target=browser --outfile=bundle.js",
    "dev": "vite"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react-dom": "^18.2.24",
    "vite": "^5.2.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
