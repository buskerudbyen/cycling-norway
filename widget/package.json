{
  "name": "sykkelveier-widget",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build:dev": "bun build index.tsx --target=browser --outdir=dist --watch",
    "build:prod": "bun build index.tsx --target=browser --outdir=dist --minify",
    "dev": "vite"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react-dom": "^18.2.24",
    "typescript": "^5.0.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
